apiVersion: v1
kind: Service
metadata:
  name: echoserver
  namespace: default
  labels:
    app: echoserver
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.1.253
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: echoserver